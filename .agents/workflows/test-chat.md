---
description: Run Playwright tests for the Spellbook chat interface
---

# Test Chat Workflow

This workflow automates the browser testing for the Spellbook clone.

// turbo

1. Start the dev server in the background:
   - Command: `npm run dev`
2. Run Playwright tests:
   - Command: `npx playwright test`
3. Stop the dev server.
